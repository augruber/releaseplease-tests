[project]
name = "releaseplease-tests"
version = "0.0.0"
description = "Test workspace for release-please experimentation"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["sdk", "toolkit", "tool_packages/*"]
